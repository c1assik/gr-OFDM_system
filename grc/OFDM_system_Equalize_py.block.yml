id: OFDM_system_Equalize_py
label: Equalize_py
category: '[OFDM_system]'

templates:
  imports: import OFDM_system
  make: OFDM_system.Equalize_py()


inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: 64

outputs:
- label: out
  domain: stream
  dtype: complex
  
#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
